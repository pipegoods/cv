---
interface Props {
  label: string;
  size?: 'small' | 'default';
}

const { label, size = 'default' } = Astro.props;
---

<span
  class={`bg-blue-100 text-blue-800 font-medium me-2 px-2.5 py-0.5 rounded` +
    (size === 'small' ? ' text-xs' : ' text-sm')}>{label}</span
>
